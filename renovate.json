{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    ":semanticCommits",
    ":dependencyDashboard",
    ":prConcurrentLimit10"
  ],
  "labels": ["dependencies"],
  "rangeStrategy": "bump",
  "commitBody": "Signed-off-by: {{{gitAuthor}}}",
  "packageRules": [
    {
      "description": "Hydrogen preview toolkit is the migration's upstream-compat contract (HYDROGEN_MIGRATION.md). Keep it pinned to an exact version and give it its own PR so the required e2e gate runs on every bump and the lib/commerce adapter is reviewed. See HYDROGEN_UPGRADE.md.",
      "matchPackageNames": ["@shopify/hydrogen"],
      "rangeStrategy": "pin",
      "addLabels": ["hydrogen-upgrade"],
      "minimumReleaseAge": "0 days",
      "schedule": ["at any time"]
    },
    {
      "description": "Group everything else into tidy PRs; these do not gate the migration.",
      "matchPackageNames": ["!@shopify/hydrogen"],
      "matchUpdateTypes": ["minor", "patch", "pin", "digest"],
      "groupName": "non-Hydrogen dependencies"
    }
  ]
}
